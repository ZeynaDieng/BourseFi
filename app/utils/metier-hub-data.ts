/**
 * Le contenu des fiches métier est chargé depuis la base (MetierPage) via `/api/site/public`.
 * Les slugs techniques pour le filtre catalogue restent dans `metier-tracks.ts`.
 */
export type { MetierHubEntry } from '~/types/metier-hub'
